# 本地覆盖，复制成 .env.local 使用（gitignored，不进仓库）。
#
# 仓库里不放任何真实账号/资源 ID：脚本运行时从 `aws sts get-caller-identity`
# 现算账号，再按固定命名规律拼出 Glue catalog ID / S3 桶名。所以**通常不需要
# 这个文件**——克隆下来直接跑，用的就是你自己凭证指向的账号。
#
# 只有两类情况需要它：
#
# 1) 多账号环境防串号。设了 EXPECT_ACCOUNT，test_all.sh / deploy_web.sh /
#    build_catalog_json.py 会在开工前核对当前凭证账号，不一致立即退出。
# EXPECT_ACCOUNT=123456789012
#
# 2) 资源名没按默认命名规律建。逐项覆盖：
# S3_TABLE_BUCKET=analytics-agent-tables
# ICEBERG_NAMESPACE=app_analytics
# ATHENA_WORKGROUP=analytics-agent-wg
# ATHENA_AGENT_WORKGROUP=analytics-agent-ro-wg
# ATHENA_CATALOG=s3tablescatalog/analytics-agent-tables
# GLUE_CATALOG_ID=123456789012:s3tablescatalog/analytics-agent-tables
# SITE_BUCKET=analytics-agent-site-123456789012-us-west-2
# SITE_REGION=us-west-2
# EDGE_STACK=analytics-agent-edge
# AWS_REGION=us-west-2
#
# 3) 治理层（L4）。设了这一项，后端就 AssumeRole 用那个最小权限角色查数：
#    user_messages 整表读不到，users.email / phone、user_profiles.birth_date
#    不在授权面里（Lake Formation 列级排除）。**不设＝用你自己的凭证**，本地开发
#    通常是 admin，那时只有 backend/db.py 的只读闸生效，没有列级边界。
#    角色和 LF 授权用 `python3 scripts/lakehouse/governance.py --apply` 建。
#    /health 的 identity 字段会回传生效身份——那是从外面看治理接上没有的唯一处。
#    设了这一项之后后端会自动切到 ATHENA_AGENT_WORKGROUP（上面那个），因为查询
#    结果 CSV 是明文行数据：两侧共用一个 workgroup 的话，agent 能从管理侧的结果
#    文件里把 LF 已经排除掉的 users.email 读回来。**不要**把上面那两个 workgroup
#    设成同一个值——那等于把列级排除从结果集这一侧整体绕过去。
# AGENT_ROLE_ARN=arn:aws:iam::123456789012:role/analytics-agent-ro
# AGENT_ROLE_NAME=analytics-agent-ro
#
# 注意两个目录 ID 的形式不同，不能互换：Athena 的 catalog 名**不带**账号前缀，
# Glue API 的 catalog ID **带**账号前缀。写串了会报 catalog 不存在。
#
# 另外：命令行上的环境变量优先于本文件——run.sh 逐行读它、但跳过环境里已经有的
# 键（不是 `set -a; source`），所以 `AWS_REGION=us-east-1 ./run.sh` 就是它看起来的
# 意思，不会被这里的值悄悄盖掉。
