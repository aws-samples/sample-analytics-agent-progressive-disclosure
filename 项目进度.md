# 项目进度

日期：2026-09-07（正确性与成本口径两处修正，见第二节第 3、5 小节；测量数据仍是 09-02／09-03 那两遍）

## 一、当前状态

项目的目标是在同一份数据、同一套题目上横向比较三种数据架构，三条 arm 互不取代。

| 阶段 | 状态 |
|---|---|
| DuckDB arm 代码层与后端对接 | 完成 |
| 数据层重灌（8000 万行量级） | 完成 |
| 三种架构的数据适配 | 完成 |
| 三条 arm 的正确性比对 | 完成 |
| 列级治理层（athena、redshift 两条 arm） | 完成 |
| 三条 arm 的时间与花费测试 | 完成 |

三条 arm 的定义：

| arm | 计算 | 存储 |
|---|---|---|
| athena | Athena（Trino 方言） | S3 Tables，Iceberg |
| duckdb | DuckDB，`ATTACH ... TYPE iceberg` | 与 athena **同一批** Iceberg 表 |
| redshift | Redshift Serverless，Data API | Redshift 托管存储 |

后端通过 `DB_BACKEND` 环境变量选择 arm，三条共用同一套 agent、同一套知识库卡片、同一套题目。

## 二、已完成的工作

### 1. DuckDB arm 的代码层与对接

DuckDB 直接挂载 athena 那条 arm 已经建好的 S3 Tables 命名空间，不复制一份数据。这带来两个结果：存储与 athena 完全同源，任何数值差异只能来自计算层；集市层建一次可供两条 arm 使用。

资源上限已钉死在 4 线程 / 4GB。不钉的话「笔记本上更快」这个结论会来自机器规格而不是引擎。

后端的 arm 分派会在 `DB_BACKEND` 拼错时直接报错，不静默回落到默认值。

### 2. 数据层重灌与三种架构适配

数据规模：

| 层 | 表数 | 行数 |
|---|---|---|
| 基表 | 35 | 79,943,758 |
| 集市层与派生层 | 13 | 11,374,524 |
| 合计 | 48 | 91,318,282 |

数据窗口为 2025-10-26 至 2026-01-24，共 91 天。

适配过程中修掉的主要一处是 Redshift 缺失整个集市层。集市层的 13 张表是按 arm 计算出来的，不从 CSV 装载，而装载脚本只覆盖 35 张基表，两份集市层 DDL 写好后仓库里没有任何环节执行过它们。表现是 27 道题里有 11 道报 `relation does not exist`。补建之后 27 道题全部可执行，8 项聚合指标与 athena 逐位相同。

Redshift 的集市层 DDL 相对通用版本有三处必要改写：`generate_series` 只能在 leader node 执行，日期轴改为 `ROW_NUMBER() OVER ()` 配合 `DATEADD`；`DISTINCT ON` 改为 `ROW_NUMBER() = 1`；带排序的数组下标取值改为 `ROW_NUMBER() = 2`。

### 3. 三条 arm 的正确性比对

比对分四层，逐层都要求全绿：

| 层次 | athena | redshift | duckdb |
|---|---|---|---|
| 金标 SQL 可执行 | 27/27 | 27/27 | 27/27 |
| 金标取值逐位对齐 | 基准 | 一致 | 一致 |
| 35 张基表全指标横向对账 | 基准 | 1 列有治理差异 | 一致 |
| L7 全量评测（真实调用模型） | 27/27 | 27/27 | 27/27 |

第二层是本阶段新增的检查。原因是「金标 SQL 在这条 arm 上跑通了」和「算出同一个数」是两件事：同一条 SQL 在三种方言上都能执行、而其中一条少算一截时，可执行性检查是全绿的。新增的横向对账工具（`eval/compare_goldens.py`）比较取值本身，并且是唯一会触及集市层计算结果的检查层。

对账结果为 27 道题全部一致，另记录两类差异，都不影响判分：

- **decimal 除法标度**（2 处）。`sum(gmv_attributed)/sum(cost)` 在两列 `decimal(14,2)` 上，Trino 得 68.31，Redshift 得 68.3051，DuckDB 得 68.30515774416658。相对差 0.0075%，判分容差 0.5%，三条 arm 均判对。
- **行序**（4 处）。金标 SQL 未写 `ORDER BY`，行序不由查询定义，多重集完全一致。

这两类是要写进架构对比结论的方言特征，不是缺陷。

第三层是把对账面从 27 道题扩到 35 张基表的每一列。之前的口径是「题目算得对」，这一层问的是「数据本身在三条 arm 上是不是同一份」——题目只碰到一部分列，没碰到的列上有差异不会被 27 道题发现。工具是 `correctness.py --arms-only`，跑在与计时同一个 Fargate 任务里，按列类型自动出聚合探针（数值列 `sum`、时间列 `min`/`max`、布尔列 `true`、每表 `count`），三条 arm 的标签逐位相同。

结果：35 张表、79,943,758 行、每条 arm 369 项指标，两处差异，整批 178.6 秒。引擎耗时合计 athena 51.4 秒、duckdb 39.3 秒、redshift 12.7 秒。

两处差异都在 `user_profiles.birth_date` 上，都是治理层的预期行为：

| 指标 | athena | duckdb | redshift |
|---|---|---|---|
| `min:birth_date` | 1966-02-08 | 1966-02-08 | 1966-01-01 |
| `max:birth_date` | 2008-01-29 | 2008-01-29 | 2008-01-01 |

`database/redshift/04_governance.sql` 里的脱敏策略是 `DATE_TRUNC('year', birth_date)`，挂的是 `TO PUBLIC`，对账用的连接也在它的作用范围内。年份逐位保留、月日归到 01-01，两个值都精确对上这个变换，所以这不是装载或计算的偏差，而是这条 arm 上治理层确实在生效的证据。除这一列之外 367 项全部逐位相同。

一处覆盖上的实话：这一层没有 CSV 真值做基准，只做了 arm 互比。`data/csv/` 里那份是早期小数据（`orders` 2000 行对当前装载的 854,140 行），当前规模下不存在可用的第三方真值。所以这一层能证明「三条 arm 是同一份数据」，不能证明「这份数据等于源文件」——后者由装载阶段的行数与指标闸门覆盖。

原始对账存于 `data/bench/correctness-20260903T093133Z.jsonl`。

这段解释以前只活在这份文档里，没有一行代码知道它，于是表级闸门和 `--values` 那一档**在
没有缺陷的时候是红的**。现在三列都登记在 `scripts/bench/correctness.py: MASKED` 里，两个
闸门共用同一份登记，判据从「跟另两条 arm 相等」换成「对得上脱敏策略自身的变换」：
`mask_email` 是常量、`mask_birthdate` 的 `DATE_TRUNC('year')` 单调（`max(trunc(x))` 就是
`trunc(max(x))`），这两条能核到值；`mask_phone` 拼第 1-3 与第 8-11 位、丢掉中间四位，不
单调，只能核形状——也就是说 `users.phone` 的跨 arm 相等性通过这条连接验不了，验的是掩码
在生效。豁免不是跳过：脱敏值对不上策略照旧报差异，明文 arm 之间也照旧互比，否则等于在全库
最敏感的三列上开了个盲区。改这一处的理由不是让输出好看：红得没道理和绿灯掩盖缺陷是同一枚
硬币的两面，2026-09-02 那次真的跨 arm 不一致形态一模一样也是两三个文本列，按旧行为它会被
埋在同样的两行里。

L7 评测结果：

| arm | 通过 | 平均耗时（秒/题） | 平均查文档次数 | 平均执行 SQL 次数 |
|---|---|---|---|---|
| athena | 27/27 | 43.2 | 2.2 | 0.8 |
| redshift | 27/27 | 47.9 | 2.5 | 0.8 |
| duckdb | 27/27 | 50.5 | 2.4 | 0.9 |

**表中的耗时不能当作性能数据。**它被模型延迟主导（每题 30 至 50 秒，引擎耗时是几十毫秒量级），三次运行都从本地发起，其中 duckdb 那条整体跨广域网，redshift 那条与一次装载并发。引擎性能数据要另测，见第三节。

### 4. 列级治理层

两条 arm 各有一套列级治理，原语不同，覆盖同样三列 PII 加同一张整表不授权：

| | athena | redshift |
|---|---|---|
| 原语 | Lake Formation 列级排除 | 动态脱敏 DDM + 角色级 GRANT |
| `users.email` | 不在授权面里，点名查报错 | `***@masked.invalid` |
| `users.phone` | 不在授权面里 | `151****0906` |
| `user_profiles.birth_date` | 不在授权面里 | `1971-01-01`，只保留出生年 |
| `user_messages` | 整表读不到 | 整表不授权，45 张表有 SELECT |
| 生效对象 | 指定的最小权限角色 | `TO PUBLIC`，不区分谁连进来 |

redshift 的脱敏对 superuser 同样生效，已实测确认。治理的说服力在这里：不是约束模型不去查 PII，而是查了也拿不到明文。

redshift 侧有一处尚未接线。治理层分两层，脱敏那层挂的是 `TO PUBLIC`，对所有访问者生效，已经落到 agent 身上；表级授权面已经定义好，但后端走的是调用者自己的 IAM 身份，没有切到 `analytics_agent_ro`，因此 `user_messages` 在这条 arm 上目前仍然查得到，实测 3,951,679 行。athena 是靠 `AGENT_ROLE_ARN` 做 AssumeRole 接上这一层的，redshift 对应的接线是一个独立步骤。

两套原语对上层的表现不同，这本身是对比结论的一部分。列级排除让错误在写 SQL 时就暴露，动态脱敏让分析可以照常做下去，例如生日保留年份后年龄段分析不受影响。

挂载不影响已经对齐的取值：27 道金标没有一道引用这三列，也没有 `SELECT *`。

duckdb 是第三种情况，而且比「粒度粗」更彻底：引擎内部一条治理原语都没有，没有用户、角色、`GRANT`、masking policy，也没有行列级安全，没有权限分离的概念。边界完全由它手里那份 IAM 凭证决定，而 IAM 对 S3 Tables 的动作粒度是命名空间和表，没有列这一级。表级既是下限也是上限，要达到列级效果只能靠物理投影，不能靠策略。

由此得到一条对比结论：DuckDB 直读 Iceberg 会绕过 Lake Formation 的列级排除。athena 的列级治理是引擎在查询时执行的，duckdb 不与 LF 集成，读的是底层 Parquet，策略在这条路径上没有执行点。含义是「LF 上配了列级排除」不等于「这一列在任何客户端都读不到」。这条结论目前从架构推出，尚未实测，坐实方法是用 athena 的最小权限角色凭证去跑 duckdb 查 PII 列。

### 5. 时间与花费测试

测量在 region 内的一个 Fargate 任务里进行，三条 arm 的客户端都在同一台机器上，网络位置与算力口径相同。这一点是前提而不是细节：从本地发起时 Athena 与 Redshift 的每次提交、轮询、取结果都走公网往返，实测接口开销 1.2 至 1.3 秒，而 DuckDB 是进程内执行没有这一趟，三条 arm 量到的不是同一样东西。同区测得的接口开销降到 0.41 至 0.58 秒。

查询集 8 条，每条覆盖一种想让引擎差异现形的形状：全表扫描聚合、单日分区剪枝、高基数分组、一对多 JOIN、最大表按天分桶、无剪枝文本匹配、窗口累计、三表 JOIN 去重计数。每条先热身一次再计时 5 次，报中位数。三条 arm 结果不一致的查询不报耗时，这是硬闸门。

耗时中位数（客户端墙上时钟，毫秒）：

| 查询 | athena | duckdb | redshift |
|---|---|---|---|
| scan-agg | 1509.8 | 63.5 | 572.5 |
| prune-1day | 1222.2 | 40.1 | 544.1 |
| groupby-highcard | 1456.1 | 97.9 | 574.7 |
| join-fanout | 2129.3 | 238.8 | 559.7 |
| events-daily | 1657.0 | 179.4 | 550.1 |
| text-like | 1210.7 | 59.3 | 525.9 |
| window-rows | 2068.6 | 74.7 | 540.4 |
| join-3way | 2272.0 | 177.3 | 547.2 |

**名次取决于量的是哪一段，这是这一节最重要的一条。** 墙上时钟拆成引擎自报与访问路径两段之后，两种排法名次不同：

| | 引擎自报 | 访问路径 |
|---|---|---|
| athena | 647 至 1762 ms | 414 至 575 ms |
| duckdb | 40 至 239 ms | 进程内，没有这一段 |
| redshift | 45 至 80 ms | 467 至 528 ms |

按引擎排是 redshift 最快，按墙钟排是 duckdb 最快。引擎差异是几十毫秒量级，访问路径差异是几百毫秒量级，在当前数据规模下后者盖掉了前者。数据再大一个量级时这个结论可能反过来。

耗时数字的稳定性单独验过：八条查询上 duckdb 最慢的一次仍快于 redshift 最快的一次，区间不重叠，名次不是噪声。两次独立的同区运行（09-02 与 09-03）中位数相互吻合，duckdb 八条落在 ±4 毫秒内。

冷启动与稳定态的比值三条 arm 差别很大，成因也不同：athena 1 至 2 倍，来自排队与元数据；redshift 1 至 32 倍，来自 workgroup 恢复；duckdb 36 倍，来自首次从 S3 拉列块。duckdb 那个倍数最大，也最容易被读成「它慢」，实际是它稳定态最快但必须先热起来，体感取决于承载它的任务是否常驻。

**冷启动单独量了一遍端到端，并与账单结算过**（2026-09-07，`cost-cold-20260907T053407Z.jsonl`）。口径是一遍 8 条、不热身、不取中位数、从任务置备算起：

| arm | 端到端墙钟 | 成本 |
|---|---|---|
| athena | 16.0s | $0.000498 |
| duckdb | 36.8s（其中 26.0s 是拉镜像 + 起容器） | $0.004156 |
| redshift | 39.2s | $0.144000（账单侧） |

**这张表的名次与稳定态那张恰好相反：冷起一遍里 athena 既最快也最便宜。** 原因是 duckdb 最擅长的那一段（执行）只占冷启动开销的十分之一，剩下的时间花在开机上。三条 arm 自身时长加起来 65.9 秒、整批墙钟也是 65.9 秒，这个吻合正是这里可以按 arm 拆账的前提。athena 与 redshift 不含开机不是漏算：它们是托管服务，从哪台机器发查询都不改账单，容器对那两条 arm 只是让延迟可比的量具；duckdb 是进程内引擎，那台机器就是它的算力。

成本分两栏报，因为三家计费形状不同，而且**三家起步价的单位各不相同**——这一点比单价重要，也是这两栏必须分开的原因：

| arm | 起步价 | 单位 | 能不能摊薄 |
|---|---|---|---|
| athena | 每条查询 10 MB | 查询 | 不能，按条落 |
| redshift | 60 秒 × 8 RPU | 活动分钟 | 部分能，跨分钟就多一段 |
| duckdb | 每个任务 60 秒（Fargate） | 任务 | 能，一遍就是一个任务 |

八条查询里有七条低于 Athena 那条 10 MB 线，意味着一条 50 毫秒的 Redshift 查询和一条 59 秒的同价，也意味着 Athena 那一栏里相同的数字反映的是「这个数据量还没进它的计量区间」，不是「这几条查询一样贵」。DuckDB 自身免费，付的是承载它的 Fargate 时长加它自己发的 S3 请求费。

| arm | 单条隔离合计 | 整批摊薄（连着跑一遍） |
|---|---|---|
| athena | $0.000498 | $0.000498 |
| duckdb | $0.031344 | $0.003963 |
| redshift | $0.384000 | $0.048000（下界，见下） |

「单条隔离」对应偶尔问一个问题，各家起步价都收满；「整批摊薄」对应跑一个作业。把前者当日常成本会高估 Redshift 二十多倍，把后者当偶发查询成本会低估它。两种用法本来就不同价，压成一个数会误导。

**摊薄那一栏在 2026-09-07 修过一次，旧数是串台的。** 之前登记的是 athena $0.000292、duckdb $0.008955、redshift $0.103871，三个数同出一个错：它们都拿整批墙上时长去算，而那段时长覆盖的是「三条 arm × 8 条查询 ×（热身 + 5 遍）」。拿它算 Redshift 的 RPU-秒等于让 Redshift 为另两条跑的时间付钱，同一段时间又整份记给了 Fargate，于是这一栏加起来超过整批实际花掉的钱。Athena 那一格错的方向相反：它的 10MB 起步价是**每条查询**收的，把字节求和之后只落一次起步价，等于漏收了 8 条里的 7 次。现在这一栏是 `timing.amortized` 这个纯函数，各 arm 用自己的中位耗时求和，并由自测钉住一条性质——**任何一条 arm 的金额都不随另一条 arm 的耗时变化**，那正是旧实现做不到的事。同一次把 Fargate 每个任务最少收 1 分钟这条规则也补上了；它的单位是任务不是查询，所以只在算整个任务生命周期的地方套，八条查询共用一个任务只碰这条线一次。

Athena 两栏相等是构造出来的结论不是算漏：它按扫描字节收钱，起步价按条落，没有可摊薄的时长。

**Redshift 那一格是下界，与账单差 3 倍，这一点是量出来的。** 2026-09-07 拿 `sys_serverless_usage` 结算了一遍冷启动：8 条查询自身耗时 37.6 秒，模型按 max(37.6, 60) 算出 $0.048，账单侧记的是 1440 RPU-秒即 $0.144。差在这条 60 秒下限的**单位**——它不是「一段活动收一次」，而是按活动分钟一段段收，跨了挂钟的分钟边界就多收一段，活动之后紧邻的空闲分钟也照收。那一遍占了 05:34 与 05:35 两分钟，加上 05:36 那个 `compute=0` 的分钟，一共 3 段。一遍会落在几个分钟上取决于查询在挂钟上的落点，从「自身耗时之和」推不出来，所以 `timing.py` 报下界并在说明里写明，账单侧真值由 `cost_cold.py --settle` 直接读那张视图给出。

DuckDB 的 S3 请求费是本轮测量补上的一项，此前记的是「未计入」，理由是请求数在客户端不可观测。这个理由在 DuckDB 1.5 之后不成立：`enable_logging('HTTP')` 会把每次请求连 URL 一起落进 `duckdb_logs_parsed('HTTP')`。实测八条查询冷态共发 1365 次请求，热态共 11 次，相差 124 倍，差异来自 `enable_object_cache` 把列块留在内存里。冷态对应每次问的都是新问题，也就是 agent 的用法，所以单条隔离那一栏按冷态算。

**补上这一项翻转了一条结论。** DuckDB 的单条隔离成本从 $0.000059 涨到 $0.000606，从比 Athena 便宜八倍变成比 Athena 贵 22%。原先那句「DuckDB 最省」只在整批作业那一栏成立。

**同一栏 2026-09-07 又修了第二次，这次漏的是开机。** 隔离栏的前提是「一条查询单独来一次」，而 DuckDB 单独来一次就得先起一个 Fargate 任务——实测 26.0 秒拉镜像起容器，加执行的 0.1 至 2.7 秒，不到 60 秒按 Fargate 的一分钟起步价收，每条 $0.003918。八条各来一次是 $0.031344，比原先登记的 $0.000606 高 52 倍。漏的形态值得记：摊薄那栏含开机、隔离栏不含，同一张表里两条 arm 口径不一致，而不一致的方向刚好偏向 DuckDB。修完之后隔离栏的排序是 athena $0.0000477 < duckdb $0.003918 < redshift $0.048，DuckDB 与 Redshift 的差距从 634 倍缩到 12 倍。这一栏它八条同价，因为收的是任务不是查询——执行那几秒在账单上看不见。

请求数是实测的，单价借用的是普通 S3 的 GET Tier-2 价，而这些请求打的是 S3 Tables 的数据端点，它自己的请求价目本项目没有取，所以这一栏的金额按量级读。Athena 与 Redshift 没有这一项不是漏测，它们的请求费在 $5/TB 与 RPU-秒里，客户端也看不到服务端发出的请求。

单价全部取自 AWS 价目 API，连 `usagetype` 与取数时刻一起存档，不写在代码里。起步价与最小计费量这类**规则**写在代码里并各自注明出处，因为价目 API 只给单价、不给规则，而在当前数据量下规则对「一条查询多少钱」的影响比单价大得多。

摊薄口径改成按 arm 各算各的之后，「测量动作本身的耗时要从整批时长里扣掉」这条讲究不再影响任何金额：整批墙钟已经不进任何一条 arm 的成本，它只是测量花掉的时间。旧文档里那句「数请求那几次执行给整批加了 21.7 秒，不扣的话 Redshift 的摊薄成本会从 $0.104 涨到 $0.135」正是串台版本才有的现象——一条 arm 的账单会随另一条 arm 多跑几遍而上涨，这本身就是那个错的症状。

报告是 trace 的函数，所以这次修正没有重跑任何云上查询：改的是算法，输入还是 2026-09-03 那一遍的实测数据，重出一次报告即可。这也是当初把成本计算放在渲染期而不是测量期的理由。

原始测量存于 `data/bench/timing-20260903T092605Z.jsonl`，报告是它的函数，改一次表述不需要重跑云上查询。

### 6. 验收栈

athena arm 的完整验收栈（L0 至 L6，加 L8 负向测试）当前全绿。L8 逐个注入缺陷，验证每个检查器在对应缺陷面前确实会变红，47 个用例全部通过。

## 三、剩余工作

暂缓项两件，均按既定优先级排在正确性之后：

- **治理能力对比**。athena 与 redshift 两条 arm 的治理层都已挂载，duckdb 没有。剩下的工作是把两套原语的差异量化成对比结论，不是补实现。
- **数据的业务真实性**。当前三条 arm 在同一份数据上算出同一个数，这一点成立；但这份数据存在若干不符合业务常识之处，例如订单明细单价与商品定价相关系数为 −0.001、13.9% 的事件时间落在所属会话窗口之外、4 张维表仍是早期规模。修正需要重新生成全量数据。

## 四、覆盖边界

「正确性比对完成」的口径是：35 张基表按列类型的聚合指标做过三方比较，27 道题的取值做过三方比较，两者合起来覆盖到基表与集市层。仍未覆盖的是两类。一是三方之外的真值：当前规模下没有可用的源文件基准，`data/csv/` 是早期小数据，所以「三条 arm 一致」不等于「等于源数据」，后者靠装载阶段的行数与指标闸门保证。二是列上的聚合指标之外的业务口径，例如逐行比对、文本列的分布、跨表引用完整性。这个边界可以继续扩，但作为「三种架构算得对不对」的阶段性结论，现有覆盖已经足够支撑。

时间与花费那一节的边界另有四条。单价里 S3 Tables 数据端点的请求价借用了普通 S3 的 GET Tier-2 价，金额按量级读。八条查询里七条低于 Athena 的 10 MB 起步线，说明当前数据量还没进入 Athena 的计量区间，量级再上去名次可能变。`timing.py` 报的 Redshift 摊薄成本是**下界**，与账单差 3 倍（对账见第五节），要账单侧真值得走 `cost_cold.py --settle`；反过来 Athena 与 DuckDB 那两栏没有做过同样的账单侧对照，它们的口径正确性目前只靠计费规则本身，不靠对账。最后，冷启动那张端到端表只有一遍样本，没有热身也没有中位数，它回答的是「第一次问要等多久、花多少」，不能当稳定态读。
